Optional: the "Build a Bouquet" game uses flower emoji by default, so this
folder can stay empty. If you'd rather use custom flower images, you can add
them here and adapt scripts/bouquet.js (the FLOWERS array).
